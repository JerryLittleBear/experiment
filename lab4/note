学到的：
1、互斥锁
2、结构体的强制类型转换
3、预处理：#ifndef和#endif
4、malloc出来的堆空间不会随着调用它的函数的结束的而被释放，只有通过free释放
5、强制类型转换的应用
6、编译没问题，但出现段错误的bug，原因是将help函数（原本无型参）定义成有一个LinkTable×型参，这样在调用的时候，因为调用的函数指针是没有型参的，所以当系统执行到help函数体时，LinkTable×相当于是没有赋值的指针，不能操作，所以出现来内存段错误。
